package Model;

/**
 * Enum of the turn status.
 */
public enum TurnStatus {
    /**
     * The turn hasn't started yet or is already over.
     */
    NOTSTARTED,

    /**
     * The turn is running.
     */
    ACTIVTURN,
}